<h1>Editing Assignment for <%= @course.name %></h1>

<%= render 'form', assignment: @assignment %>

<%= link_to 'Show', course_assignment_path(@course, @assignment) %> |
<%= link_to 'Back', course_assignments_path(@course) %>
